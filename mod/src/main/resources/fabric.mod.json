{
    "schemaVersion": 1,
    "id": "${id}",
    "version": "${version}",
    "name": "${name}",
    "description": "${description}",
    "authors": [
        "${authors}"
    ],
    "contact": {
        "homepage": "${website}",
        "sources": "${source_url}",
        "issues": "${issue_tracker}"
    },
    "license": "${license}",
    "icon": "${icon}",
    "environment": "server",
    "entrypoints": {
        "server": [
            "${fabric_server_entry}"
        ]
    },
    ${ dependencies_field }
}